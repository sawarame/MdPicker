# MdPicker — Googleウェブストア掲載用説明文

> Googleウェブストアの文字数制限は、**短い説明: 132文字以内**、**詳細な説明: 16,000文字以内**です。

---

## 🇺🇸 英語（English）

### **Name**
MdPicker - Markdown Link Copier

### **Short Description**
**118 characters ✅**
```
Copy links and pages as Markdown, Slack, Scrapbox, or HTML. Supports custom templates, favicons, and keyboard shortcuts.
```

### **Detailed Description**
```
MdPicker is a lightweight Chrome extension that adds powerful link copying and conversion options to your right-click context menu. With a single click or a keyboard shortcut, you can convert any web link, selected text, or page into clean Markdown, Slack, Scrapbox, HTML, or your own custom formats.

[ CORE FEATURES ]

- Copy Link as Markdown
Automatically extracts the link's display text and URL to create a clean [Link Text](URL) format.

- Copy Selection as Markdown
Intelligently analyzes the HTML structure (headings, bold, lists, tables, etc.) of your selected text and converts it into accurate Markdown while preserving the original formatting. Automatically resolves relative image paths and links to absolute URLs to prevent broken resources when pasting.

- Copy Page as Markdown Link
Instantly creates a Markdown link of the current page's title and URL.

- Customizable Format Templates (New!)
Export links in various formats, including Slack (<URL|Title>), Scrapbox ([URL Title]), HTML (<a href="URL">Title</a>), and Markdown with dates. Create and manage custom templates using dynamic placeholders: ${title}, ${url}, ${date}, ${time}, and ${favicon}.

- Dedicated "Copy as..." Context Menus (New!)
Access your templates directly from the right-click menu using "Copy Link as..." and "Copy Page as..." sub-menus.

- Favicon Support
Include the page's favicon image in your page links. Enable this in the options and choose from 4 sizes: 12px, 16px, 24px, or 32px.

- Smart Keyboard Shortcut
Use Ctrl+Shift+M (Cmd+Shift+M on Mac) to copy your selection as Markdown. If nothing is selected, it copies the current page using your chosen default format.

[ 💡 HOW TO USE ]

1. Copying a link: Hover over any link, right-click, and select "Copy Link as Markdown" or choose a format from "Copy Link as...".
2. Copying selected text: Select text on a page, right-click, and select "Copy selection as Markdown" (or use the shortcut).
3. Copying the page link: Right-click on an empty space and select "Copy Page as Markdown Link" or choose from "Copy Page as...".
4. Customizing templates & settings: Right-click the extension icon and select "Options" to add/delete templates, choose your default format, and configure favicon settings.

[ DEVELOPER & POWER USER FEATURES ]

- Manifest V3 Compliant: Built using modern Chrome extension standards for high security and performance.
- Offscreen Document API: Uses official APIs for reliable clipboard access.
- Flexible Placeholders: Easily configure formats for Notion, Slack, Scrapbox, HTML, or personal wikis.
- Cross-Device Sync: Uses the Chrome Storage API to sync templates and preferences across your devices.
```

---

## 🇯🇵 日本語（Japanese）

### **Name**
MdPicker - Markdown形式でリンクをコピー

### **Short Description**
**72文字 ✅**
```
リンクやページをMarkdown、Slack、Scrapbox、HTML形式で右クリックからコピー。カスタムテンプレートやfavicon付与にも対応。
```

### **Detailed Description**
```
MdPicker は、Chrome の右クリックメニューに強力なリンク変換・コピー機能を追加する、軽量な拡張機能です。クリック一回またはキーボードショートカットで、Webページ上のリンク、選択テキスト、または現在のページの情報を Markdown、Slack、Scrapbox、HTML、および自作のカスタム形式に変換してクリップボードにコピーできます。

【 主な機能 】

■ リンクを Markdown 形式でコピー
リンクの表示テキストと URL を自動抽出し、[表示テキスト](URL) の形式でコピーします。

■ 選択範囲を Markdown としてコピー
Webページ上の見出し、太字、箇条書き、テーブルなどの HTML 構造を解析。元の書式を維持したまま正確な Markdown 記法に変換します。コピー時に画像やリンクの相対パスを絶対URLに自動変換するため、貼り付け先での画像表示エラーやリンク切れを防ぎます。

■ このページをリンクとしてコピー
現在のページのタイトルと URL から、即座に [ページタイトル](URL) の形式でリンクを作成します。

■ フォーマットテンプレート機能（新機能！）
Markdown に加え、Slack（<URL|タイトル>）、Scrapbox（[URL タイトル]）、HTML（<a href="URL">タイトル</a>）、日付付き Markdown などのプリセットを標準搭載。さらに、${title}、${url}、${date}、${time}、${favicon} の変数を自由に組み合わせて、オリジナルのテンプレートを作成・管理できます。

■ 「別形式でコピー」サブメニュー（新機能！）
右クリックメニューに「リンクを別形式でコピー」および「このページを別形式でコピー」のサブ階層を用意。用途に応じて任意のテンプレート形式を選択して素早くコピーできます。

■ favicon（ファビコン）対応
ページリンクの先頭に favicon 画像を含めることができます。オプション画面で有効化でき、サイズも 12px / 16px / 24px / 32px から選択可能です。

■ スマート・キーボードショートカット
Ctrl+Shift+M (Mac では Cmd+Shift+M) を押すだけでコピー可能。テキスト選択時は選択範囲の Markdown を、未選択時は現在のページ情報を設定されたデフォルト形式でコピーします。

【 💡 使い方 】

1. リンクをコピー：リンク上で右クリックし「リンクを Markdown としてコピー」または「リンクを別形式でコピー」から希望の形式を選択。
2. 選択範囲をコピー：テキストを選択して右クリックし「選択範囲を Markdown としてコピー」を選択（またはショートカット）。
3. ページリンクをコピー：ページ上の何もない場所を右クリックして「このページをリンクとしてコピー」または「このページを別形式でコピー」から選択。
4. 設定変更：拡張機能アイコンを右クリックし「オプション」を選択して、テンプレートの追加・削除、デフォルト形式の選択、favicon 設定を変更。

【 開発者・パワーユーザー向けの特長 】

・Manifest V3 準拠：最新の Chrome 拡張規格に基づき、高いセキュリティとパフォーマンスを実現しています。
・Offscreen Document API：最新の API を使用し、安全かつ確実にクリップボードへアクセスします。
・柔軟なテンプレート機構：Notion、Slack、Scrapbox、ブログ、社内Wikiなどの用途に合わせて自由に書式を定義できます。
・設定の同期：Chrome Storage API を利用し、保存したテンプレートや設定はデバイス間で自動同期されます。
```

---

## 📋 ウェブストア登録時のチェックリスト

| 項目 | 内容 |
|------|------|
| **カテゴリ** | Productivity（生産性） |
| **言語** | English（主言語）+ 日本語（追加ロケール） |
| **スクリーンショット** | 右クリックメニューの表示・オプション画面・コピー結果のビフォーアフターを推奨 |
| **プロモーション画像** | 1280×800px（任意）、440×280px（小バナー・任意） |
| **アイコン** | 128×128px（必須） — `icon128.png` ✅ |
