{
  "name": "md-picker",
  "version": "1.0.3",
  "description": "Copy links as Markdown",
  "scripts": {
    "build": "rimraf ./MdPicker/js && rimraf MdPicker.zip && webpack --mode production && (cd MdPicker && zip -r ../MdPicker.zip .)",
    "dev": "webpack --mode development --watch",
    "generate-icons": "node scripts/generate-icons.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.280",
    "@types/turndown": "^5.0.6",
    "rimraf": "^6.1.3",
    "sharp": "^0.34.5",
    "ts-loader": "^9.5.1",
    "typescript": "^5.4.5",
    "webpack": "^5.91.0",
    "webpack-cli": "^5.1.4"
  },
  "dependencies": {
    "turndown": "^7.2.4",
    "turndown-plugin-gfm": "^1.0.2"
  }
}